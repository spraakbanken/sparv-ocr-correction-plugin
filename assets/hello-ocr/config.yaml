metadata:
    id: hello-ocr
    language: swe

import:
    importer: text_import:parse

export:
    annotations:
        - <sentence>
        # - <token:word>
        - <token>:stanza.pos
        - <token>:ocr_suggestion.ocr-suggestion

sparv:
    compression: none
